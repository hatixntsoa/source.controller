#!/bin/bash

# Arguments for the usage
gst_arguments=(
  "$(basename "$0" .sh)"
)

# Description for the usage
gst_descriptions=(
  "This script simplifies the process of viewing"
	"local git repository status."
)

# Options for the usage
gst_options=(
  "--help            Display this help message."
)

# Extra help
gst_extras=(
  "No arguments are required. Just run the script to view status."
)
